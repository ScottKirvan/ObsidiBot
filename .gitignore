.obsidian/*
node_modules/
main.js
_obsidibot-debug.log
# Generated at runtime when plugin dev folder is opened directly as a vault
/obsidian-commands.md
/commands.md
obsidibot-debug.log
# Pasted images saved by ObsidiBot clipboard paste feature
tmp/

# Obsidian plugin runtime data (machine-specific)
data.json

# Claude Code — machine-specific and runtime files
.claude/settings.local.json
.claude/settings.json
# ObsidiBot runtime data written when plugin runs from symlinked dev folder
# In production vaults this lives at .obsidian/plugins/obsidibot/.claude/sessions/
.claude/sessions/

# User slash command templates (local to each install)
commands/

# TypeScript compiled output (esbuild bundles to main.js; tsc -noEmit only)
src/**/*.js
test/**/*.js

notes/COMMIT_DRAFT.md
ObsidiBot_Marketing/
docs/.vitepress/cache/
cortex-debug.log
